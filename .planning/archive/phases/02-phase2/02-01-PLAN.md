---
phase: 02-phase2
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - "EPUB/PDF outputs are derived from DOCX, and DOCX is derived from the single primary Markdown file per repo"
    - "Workflow logic should be shared to reduce drift across the four canonical repos"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 dependency and exit checklist criteria"
    - path: "docs/current-metrics.md"
      provides: "Cross-repo baseline facts to keep aligned during implementation"
  output: "Shared docs-generation workflow/composite action adopted in all four canonical repos"
---

<objective>
Implement Plan 02-01 by centralizing docs-generation workflow logic and validation gates across all four canonical document repositories.
</objective>
<execution_context>
GSD execute-plan workflow
</execution_context>
<tasks>
  <task type="auto">
    <name>Task 1: Design shared workflow contract [PRE-SATISFIED]</name>
    <files>ai-assisted-docs: .github/workflows/reusable-generate-docs.yml</files>
    <action>COMPLETE — Reusable workflow committed 2026-03-12 (a8194c4). Contract is the workflow_call inputs block: primary_markdown (required), artifact_base, toc_depth, commit_message. Bootstraps reference.docx, pdf-defaults.yaml, and epub.css if missing.</action>
    <verify>Workflow file exists and defines all required inputs.</verify>
    <done>Shared contract documented and accepted</done>
  </task>
  <task type="auto">
    <name>Task 2: Roll out shared workflow</name>
    <files>All four canonical repo .github/workflows/generate-docs.yml files</files>
    <action>COMPLETE — 2026-03-12 rollout committed and pushed in all four canonical repos. Replaced each repo's duplicated generate-docs workflow with a thin caller to `dknauss/ai-assisted-docs/.github/workflows/reusable-generate-docs.yml@main`, preserving repository-specific `primary_markdown` filenames and output filenames. Latest rollout commits: Benchmark `79f56f3`, Hardening `1a071c7`, Runbook `4723f96`, Style Guide `3a9ca55`.</action>
    <verify>`generate-docs` succeeded in all four repos after migration: Benchmark run `23028345218`, Hardening run `23028345314`, Runbook run `23028345390`, Style Guide run `23028345437`. `release.yml` remained unaffected because output filenames were preserved. Existing `validate-metrics.yml` + `verify-metrics.sh` remained functional and latest runs stay green.</verify>
    <done>All four repos are on shared generation logic</done>
  </task>
  <task type="auto">
    <name>Task 3: Validate and record</name>
    <files>docs/current-metrics.md; .planning/STATE.md</files>
    <action>COMPLETE — 2026-03-12 planning state, roadmap, and project metrics updated with rollout evidence, run URLs, and residual follow-up limited to manual `reference.docx` page numbering under Plan `02-02`.</action>
    <verify>State shows `02-01` complete with references and `02-02` as next active plan.</verify>
    <done>02-01 completion evidence recorded</done>
  </task>
</tasks>
<verification>
  <notes>Do not start Plan 02-02 until 02-01 is complete in all four repos.</notes>
</verification>
<success_criteria>
  <criteria>One shared workflow/composite action pattern is adopted in all four canonical repositories</criteria>
  <criteria>Generation scope remains restricted to each repo's single primary Markdown file</criteria>
  <criteria>`generate-docs` and `validate-metrics` are green in all four canonical repositories</criteria>
</success_criteria>
<output>PLAN 02-01 completed and recorded on 2026-03-12</output>
