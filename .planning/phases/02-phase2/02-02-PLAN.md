---
phase: 02-phase2
plan: 02-02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Plan 02-01 must be completed before output-polish rollout begins"
    - "DOCX remains the intermediary template layer for final EPUB/PDF outputs"
  artifacts:
    - path: ".planning/phases/02-phase2/02-01-PLAN.md"
      provides: "Dependency gate for shared workflow adoption"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 exit checklist criteria"
  output: "Output-polish baseline (reference.docx + EPUB CSS) implemented with validation"
---

<objective>
Implement Plan 02-02 by defining and rolling out output-polish standards while preserving DOCX-intermediary derivation and generation guardrails.
</objective>
<execution_context>
GSD execute-plan workflow
</execution_context>
<tasks>
  <task type="auto">
    <name>Task 1: Define polish baseline [EDITORIAL DECISIONS MADE]</name>
    <files>Canonical repo .github/pandoc/ directories</files>
    <action>Polish baseline decisions (2026-03-12):
    - EPUB CSS: Current baseline in reusable workflow is adequate. No changes needed.
    - reference.docx: Pandoc default is adequate for styling. Page numbering must be added (open in Word, Insert > Page Number > Bottom of Page) in all four repos.
    - pdf-defaults.yaml: Shared bootstrap baseline is already accepted from Plan 02-01 and should be treated as an input, not redefined here.
    Document these decisions and verify each repo's pandoc assets match the accepted baseline.</action>
    <verify>All four repos have reference.docx with page numbering and epub.css matching the reusable workflow baseline. Shared `pdf-defaults.yaml` behavior from 02-01 remains intact.</verify>
    <done>Polish baseline documented and accepted</done>
  </task>
  <task type="auto">
    <name>Task 2: Apply baseline via shared pipeline</name>
    <files>Shared workflow/composite action and canonical repo .github/pandoc/ assets</files>
    <action>Apply page numbering to each repo's reference.docx and regenerate all four document sets through the shared pipeline. Do not introduce new pipeline logic here; this plan validates the accepted polish baseline on top of the migrated shared workflow.</action>
    <verify>Regenerated DOCX includes page numbers. PDF includes header (section name) and footer (page number). EPUB unchanged (adequate). `release.yml` unaffected.</verify>
    <done>Polish baseline active in all repos</done>
  </task>
  <task type="auto">
    <name>Task 3: Validate and close Phase 2</name>
    <files>.planning/ROADMAP.md; .planning/STATE.md; docs/current-metrics.md</files>
    <action>Run Phase 2 exit checklist (including release.yml verification and validate-metrics.yml confirmation). Capture validation evidence and mark phase completion status.</action>
    <verify>Roadmap progress table and state reflect completed 02-01/02-02 with evidence links. All exit checklist items satisfied.</verify>
    <done>Phase 2 closed with verification artifacts</done>
  </task>
</tasks>
<verification>
  <notes>Any styling changes must be tested on generated PDF and EPUB outputs and must not re-enable generation for non-primary Markdown files.</notes>
</verification>
<success_criteria>
  <criteria>Reference DOCX and EPUB CSS baselines are documented and enforced</criteria>
  <criteria>All four repos pass generation and validation gates after rollout</criteria>
  <criteria>Phase 2 exit checklist is fully satisfied and recorded</criteria>
</success_criteria>
<output>PLAN 02-02-PLAN.md created</output>
