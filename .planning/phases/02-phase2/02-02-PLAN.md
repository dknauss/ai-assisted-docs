---
phase: 02-phase2
plan: 02-02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Plan 02-01 must be completed before output-polish rollout begins"
    - "DOCX remains the intermediary template layer for final EPUB/PDF outputs"
  artifacts:
    - path: ".planning/phases/02-phase2/02-01-PLAN.md"
      provides: "Dependency gate for shared workflow adoption"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 exit checklist criteria"
  output: "Output-polish baseline (reference.docx + EPUB CSS) implemented with validation"
---

<objective>
Implement Plan 02-02 by defining and rolling out output-polish standards while preserving DOCX-intermediary derivation and generation guardrails.
</objective>
<execution_context>
GSD execute-plan workflow
</execution_context>
<tasks>
  <task type="auto">
    <name>Task 1: Define polish baseline</name>
    <files>Canonical repo pandoc configs and docs</files>
    <action>Define a standard `reference.docx` styling baseline and EPUB CSS baseline (typography, spacing, heading hierarchy, table rendering, code block formatting).</action>
    <verify>Baseline documented with explicit acceptance checks and fallback behavior.</verify>
    <done>Polish baseline approved</done>
  </task>
  <task type="auto">
    <name>Task 2: Apply baseline via shared pipeline</name>
    <files>Shared workflow/composite action and canonical repo references</files>
    <action>Implement baseline through the shared docs-generation path established by Plan 02-01.</action>
    <verify>Regenerated DOCX/PDF/EPUB artifacts in all four repos include baseline styling changes.</verify>
    <done>Polish baseline active in all repos</done>
  </task>
  <task type="auto">
    <name>Task 3: Validate and close Phase 2</name>
    <files>.planning/ROADMAP.md; .planning/STATE.md; docs/current-metrics.md</files>
    <action>Run Phase 2 exit checklist, capture validation evidence, and mark phase completion status.</action>
    <verify>Roadmap progress table and state reflect completed 02-01/02-02 with evidence links.</verify>
    <done>Phase 2 closed with verification artifacts</done>
  </task>
</tasks>
<verification>
  <notes>Any styling changes must be tested on generated PDF and EPUB outputs and must not re-enable generation for non-primary Markdown files.</notes>
</verification>
<success_criteria>
  <criteria>Reference DOCX and EPUB CSS baselines are documented and enforced</criteria>
  <criteria>All four repos pass generation and validation gates after rollout</criteria>
  <criteria>Phase 2 exit checklist is fully satisfied and recorded</criteria>
</success_criteria>
<output>PLAN 02-02-PLAN.md created</output>
